# Target: Miniconda Python 3.10 on the RunPod host (A40, driver CUDA 12.8).
# Do not install into the image base env (PyTorch 2.8 / Python 3.12): torch 1.13.1
# has no wheels there and would conflict with the template.
#
# CUDA 12.8 drivers run the cu117 wheels (driver backward compatibility).
--extra-index-url https://download.pytorch.org/whl/cu117
torch==1.13.1
torchvision==0.14.1

# Keep the original LISA Llama/LLaVA APIs. Newer transformers change
# LlamaModel.forward, generate() hidden_states, and use_cache behavior.
transformers==4.31.0
tokenizers==0.13.3
# transformers 4.31 allows huggingface-hub<1.0, which now resolves to 0.26+
# and removes cached_download. Pin a Hub client from the 4.31 era.
huggingface-hub>=0.19.4,<0.23.0
# Faster Hub downloads when HF_HUB_ENABLE_HF_TRANSFER=1 in .env
hf_transfer>=0.1.4,<0.2.0
# Required by from_pretrained(..., low_cpu_mem_usage=True) in chat.py.
# Also keeps peak CPU RAM down when loading the ~54 GB fp32 LISA shards.
accelerate==0.21.0
safetensors>=0.3.1,<0.5.0
peft==0.4.0
sentencepiece>=0.1.99
python-dotenv>=1.0.0,<2.0.0

einops==0.4.1
numpy==1.24.2
scipy==1.11.2
# Headless avoids libGL.so.1 errors on typical GPU pods. cv2 API is unchanged.
opencv-python-headless==4.8.0.74
Pillow==9.4.0
pycocotools==2.0.6

# Gradio 3.39 / FastAPI 0.100 require Pydantic v1.
# Do not let pip pull a current gradio_client: 1.x/2.x removed serializing.py
# which Gradio 3.39 still imports.
pydantic>=1.10.13,<2.0.0
fastapi==0.100.1
gradio==3.39.0
gradio_client==0.3.0
# Gradio 3.39 still does `import pkg_resources`. setuptools 82+ dropped it.
setuptools>=65.0.0,<81.0.0
bleach>=6.0.0,<7.0.0
markdown2==2.4.10
openai==0.27.8
Requests==2.31.0
shortuuid==1.0.11
tqdm==4.64.1
uvicorn==0.23.2
ray==2.6.1
packaging

# Training-only (not required for chat.py --precision=bf16):
#   pip install deepspeed==0.9.5 tensorboard
# 4bit/8bit only. Do NOT install bitsandbytes==0.41.1 on CUDA 12.8: it loads
# libbitsandbytes_cuda117.so and then fails looking for libcusparse.so.11.
# flash-attn is only used by LLaVA train_mem.py, not by chat.py / train_ds.py.
